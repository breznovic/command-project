import React from 'react';
import Checkbox from "../../../common/checkbox/Checkbox";

const FilterForId = () => {
    return (
        <p>
            <button>My</button>
            <button>All</button>

        </p>
    );
};

export default FilterForId;